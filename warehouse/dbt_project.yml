name: switchboard
version: "1.0.0"
profile: switchboard
model-paths: ["models"]
seeds:
  switchboard:
    +schema: analytics
models:
  switchboard:
    staging:
      +materialized: view
      +schema: analytics
    identity:
      +materialized: table
      +schema: analytics
    marts:
      +materialized: table
      +schema: analytics
